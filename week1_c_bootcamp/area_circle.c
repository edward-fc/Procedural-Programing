#include <stdio.h>
int main(){
    float radius;
    float pi = 3.14;
    float results;
    scanf("%f",&radius);
    results=pi*radius*radius;
    printf("the area of the circlce is %f\n",results);
}