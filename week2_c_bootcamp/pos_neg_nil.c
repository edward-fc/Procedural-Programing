#include <stdio.h>
int main(){
    int a = 0;
    if (a == 0)
    {
        printf("the number entered is equal to 0\n");
    }else if (a >= 0)
        {
            printf("the number entered is postitif\n");
        }else{
            printf("the number entered a negtive\n");
        }
        return 0;
    }